import React from "react";
import ReactDOM from "react-dom";

const modalRoot = document.querySelector("#modal-root");

const AddTask = () => {
  return (<div></div>), modalRoot;
};

export default AddTask;
